package com.davidwilliston.copy_fit

import io.flutter.embedding.android.FlutterFragmentActivity

// FlutterFragmentActivity (not FlutterActivity) is required so the health plugin
// can use registerForActivityResult for the Health Connect permission flow.
class MainActivity : FlutterFragmentActivity()
